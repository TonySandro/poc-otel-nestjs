apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nests-metrics.fullname" . }}-test-connection"
  labels:
    {{- include "nests-metrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nests-metrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
